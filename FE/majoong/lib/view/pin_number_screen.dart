import 'package:flutter/material.dart';
class PinNumberScreen extends StatelessWidget {
  const PinNumberScreen({Key? key}) : super(key: key);

  @override

  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('pin number screen'),
      ),
    );
  }
}
